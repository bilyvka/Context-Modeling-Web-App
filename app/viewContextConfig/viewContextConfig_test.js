'use strict';

describe('myApp.viewContextConfig module', function() {

  beforeEach(module('myApp.viewContextConfig'));

  describe('viewContextConfig controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});